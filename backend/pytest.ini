[pytest]
addopts = -m "not checkpoints" --strict-markers
markers =
    checkpoints: requires the real .pth files under models/ (~1.1 GB, slow)
    checkpoints_small: requires only best_epoch_4_acc_98.70.pth (~131 MB)
testpaths = tests
